# Fine Ludens 홈페이지 — Claude Code 작업 지침

이 폴더는 **빌드 없는 순수 정적 사이트**입니다. `handoff/`를 그대로 GitHub Pages 루트에 올리면 동작합니다.
프레임워크·번들러·npm 없음. 파일을 브라우저로 직접 열어도 동작합니다.

## 절대 규칙

1. **빌드 도구를 도입하지 마십시오.** 호스팅이 GitHub Pages이고 배포 파이프라인이 없습니다. React·Vite·Tailwind 도입 제안 금지.
2. **레이아웃 CSS는 `assets/css/site.css` 한 곳에만.** 페이지별 `<style>` 블록을 새로 만들지 마십시오.
3. **개별 요소의 스타일은 인라인 `style=""`으로 유지합니다.** 이 사이트는 그렇게 설계되어 있습니다. 클래스 체계를 새로 도입해 리팩터링하지 마십시오 — 디자인 편집 도구와의 호환이 깨집니다.
4. **`data-r="…"` 속성은 레이아웃 훅입니다.** 이름을 바꾸거나 지우지 마십시오. `site.css`가 이 속성으로 반응형·페이지 구조를 제어합니다.
5. **콘텐츠의 사실관계를 창작하지 마십시오.** 실적·수치·파트너 관계는 `DESIGN-NOTES.md`의 "콘텐츠 진실" 항목만 사용합니다. 없는 수상 이력·언론 보도·고객 수를 만들어 넣으면 안 됩니다.
6. 문구는 한국어 기본, 영문은 디스플레이(제목·레이블) 용도로만.

## 링크 규칙

페이지 간 링크는 **디렉터리형(`product/`)이 아니라 명시 파일 경로**(`product/index.html`)로 씁니다.
GitHub Pages·로컬 서버·file:// 어디서나 동일하게 열립니다. 루트에서는 `index.html`, 하위 폴더에서는 `../index.html` 형태입니다.

## 페이지 구조

| 경로 | 파일 | 역할 |
|---|---|---|
| `/` | `index.html` | 홈. 5개 섹션 |
| `/product/` | `product/index.html` | Fine Play 제품 소개. 6개 섹션 |
| `/company/` | `company/index.html` | 회사의 의미·미션·비전·핵심가치·연혁·뉴스. 10개 섹션 |
| `/profile/` | `profile/index.html` | 기업소개서. **8장 PPT형 카탈로그** |
| `/contact/` | `contact/index.html` | 문의. 3개 섹션 |

## 두 가지 페이지 모델 (절대 섞지 마십시오)

### 1) 연속 스크롤 페이지 — 홈 / 제품 / 회사 / 문의  (`assets/css/site.css`)
기존 fineludens.kr과 같은 **평범한 웹페이지**입니다. 다만 밀도·타이포·리듬을 크게 올렸습니다.

```
<div data-r="page">     ← 하나의 섹션. 높이 자유(고정 높이 금지)
  <div data-r="pad">    ← 본문. padding 108px 56px
```
- 스크롤 스냅 없음, `height: 100svh` 없음, `overflow: hidden` 없음.
- 연출은 **스크롤 진입 기준**(`animation-timeline: view()`, `animation-range: entry N% cover M%`).
- 섹션 간 크로스페이드(`enterSlide`/`exitSoft`)를 쓰지 마십시오 — PPT형 전환은 기업소개서 전용입니다.
- 사업자 정보는 **문서 끝의 검정 푸터 밴드**로 둡니다.
- 세로 치수는 px/clamp로 씁니다. `vh` 기반 타이포는 이 모델에서 쓰지 않습니다.

### 2) 적층 카탈로그 — 기업소개서만  (`assets/css/profile.css`)

**2026-08-13 전면 재구성.** 이전의 `scroll-snap` 방식은 폐기되었습니다.
스냅은 전환 구간을 건너뛰므로 스크롤이 크로스페이드를 진행시킬 수 없었습니다.

```
<section data-r="page">   ← 패널 8개. position: sticky; top: 0;
                             height: 100svh; margin-bottom: -100svh; overflow: clip;
  <div data-r="rail">     ← 좌측 컬럼(챕터 라벨)
  <div data-r="pad">      ← 세로 중앙 정렬
<div data-r="runway">     ← 스크롤 활주로. height: 1600svh
```

**핵심은 `margin-bottom: -100svh`입니다.** 패널의 흐름 높이가 0이 되어 8개가
처음부터 같은 자리에 겹칩니다. 흐름에 남겨두면 자기 차례 전에 아래에서 위로
올라온 뒤에야 고정되고, 그 이동 구간이 그대로 보입니다.

스크롤 거리는 `[data-r="runway"]`가 전담합니다. 패널 8개 = 슬롯 8개,
슬롯당 200svh. 이 값 하나가 전체 페이싱을 결정합니다.

- 패널 i 슬롯 = `i*12.5% ~ (i+1)*12.5%` (문서 전체 스크롤 기준)
- 앞 18%에서 `enterSlide`로 떠오르고, 나머지에서 내부 컴포넌트가 도착
- `panelPast`가 다음 패널 도착 시점에 `visibility: hidden` — 적층 구조에서
  덮인 패널이 탭 순서에 남는 포커스 함정을 막습니다. 빼지 마십시오.

**앵커 이동**: 패널이 전부 같은 흐름 위치라 `#chNN` 앵커가 무의미합니다.
id는 활주로 안의 위치 지정 `<span>`에 있습니다. 착지 지점은 각 슬롯의 94%,
즉 그 패널의 컴포넌트가 전부 나온 뒤입니다.

- 새 패널 추가 시: 활주로 높이, 슬롯 퍼센트, 인덱스 항목, 활주로 앵커,
  페이저 링크를 함께 갱신하십시오.
- 사업자 정보는 마지막 화면 안에 글씨만.

## 색면 규칙 (연속 스크롤 4면)

- **페이지 전체가 종이 한 장(`#f5f0e8`)입니다.** 섹션마다 배경색을 바꾸면 페이지가 잘려 보입니다 — 딥그린·베이지 색면을 다시 넣지 마십시오.
- 색이 바뀌는 지점은 **문서 맨 끝 한 곳**뿐입니다: 오렌지 클로징 CTA + 검정 푸터.
- 섹션 구분선(헤어라인)도 없습니다. **여백 84px + 아이브로 라벨 + 타이포 위계**만으로 구분합니다. 경계선을 다시 넣지 마십시오.
- `[data-r="wash"]`: 스크롤에 묶여 아주 느리게 흐르는 고정 광원 층. 페이지 전체를 하나로 묶는 장치이므로 유지하십시오.
- 카드·지표 배경은 `rgba(21,24,18,0.04)` 수준의 미세한 톤만 씁니다.
- 파트너 로고월은 **타일 배경 없이 종이 위에 직접** 놓습니다. 로고는 원본 색상 유지, 높이 74px. 흰색 로고(ITOP21·WK)에만 `drop-shadow` 후광을 걸어 종이 위에서 읽히게 합니다. 로고를 흑백·반전 처리하거나 색 타일 위에 얹지 마십시오 — 색 타일은 밝은 페이지 중간에 띠를 만들어 페이지가 잘려 보입니다.

## 모션 시스템

**타임라인은 두 체계입니다. 섞지 마십시오.**

| 모델 | 타임라인 | 범위 |
|---|---|---|
| 연속 스크롤 4면 | `animation-timeline: view()` | `cover N% cover M%` — 요소가 뷰포트를 지나는 진행률 |
| 기업소개서 | `animation-timeline: scroll(root)` | 절대 퍼센트 — 문서 전체 스크롤 기준 |

기업소개서가 `scroll(root)`인 이유: 패널이 `position: sticky`로 고정되면
그 요소의 `view()` 진행률이 멈춰 애니메이션이 죽습니다. 문서 스크롤은
고정 여부와 무관하므로 적층 구조에서 유일하게 안전한 기준입니다.

**`overflow: hidden`을 쓰지 마십시오. `overflow: clip`을 쓰십시오.**
`hidden`은 스크롤 컨테이너를 생성하고, 그러면 그 안의 `view()` 타임라인이
문서가 아니라 그 요소를 기준으로 잡혀 진행률이 0에 고정됩니다.
이 사이트에서 두 번 발생한 버그입니다.

**종료 지점은 도달 가능해야 합니다.** 문서 끝에 가까운 섹션은 아래 콘텐츠가
부족해 `cover 100%`에 닿지 못합니다. 연속 스크롤 4면의 종료값이 44~58%에
모여 있는 것은 그 때문입니다. 올리면 애니메이션이 중간에 얼어붙습니다.

| 키프레임 | 쓰임 |
|---|---|
| `maskUp` | 제목. 줄 단위 스태거 + 오버슈트 |
| `maskWipeX` | 라벨·리드. 좌→우 와이프 |
| `techIn` | 보조 텍스트. 흐림에서 초점 |
| `scanIn` | 이미지. 위→아래 공개 |
| `liftIn` | 카드·타일 |
| `wipeUp` | 선언문 블록 |
| `slideL` | 순서가 있는 항목(연혁 등) |
| `counterUp` | 지표 숫자 (`assets/js/count.js`) |
| `evolveIn` | 대형 장식 배경. `translateY(-50%)` 정렬을 모든 프레임에 유지 |
| `coverDive` / `coverVeil` / `coverLift` | 커버 이탈. 3층 시차 |
| `heroFocus` / `heroRise` | 커버 진입(시간 기반). 2200ms 동시 착지 |
| `enterSlide` / `panelPast` | 기업소개서 패널 도착·이탈 |

**한 섹션에 같은 등장을 반복하지 마십시오.** 움직임은 그 섹션이 무엇인지
말해야 합니다. 연혁은 `slideL`(시간의 진행), 문제 섹션은 `techIn`(초점),
선언문은 `wipeUp`입니다.

**모션은 항상 재생됩니다(사용자 지시, 2026-08-13).**
`prefers-reduced-motion` 게이트와 헤더 MOTION 체크박스는 제거되었습니다.
되돌리려면 `site.css`의 주석 블록을 해제하십시오. 접근성 후퇴임을 인지하고
내린 결정입니다.

## 공통 규칙 (5면 전체에 동일 적용 — 한 면만 바꾸지 마십시오)

| 요소 | 규칙 |
|---|---|
| 커버 사진 | 패널 전체 배경(`position:absolute; inset:0; object-fit:cover`) + 크림 스크림 그라디언트. 사진을 본문 안 작은 박스로 넣지 않습니다. |
| 사업자 정보 푸터 | 검정 바 없이 **글씨만**. 마지막 패널(오렌지) 하단에 hairline 1px으로 구분. |
| 파트너 로고 | 타일 없음(투명), 높이 74px, 원본 색상. 흰색 로고에만 `drop-shadow` 후광. |
| 이미지 크롭 | 장면 판별이 필요한 사진은 `object-fit: contain`으로 전체 프레임 노출. 배경으로 쓸 때만 `cover`. |
| 좌측 인덱스(기업소개서) | 각 패널의 `--pN` 타임라인 + `timeline-scope`로 현재 챕터 자동 강조. 클릭하면 해당 패널로 이동. JS 없음. |


## 시맨틱 구조 (2026-08-13 추가)

CSS는 `[data-r="..."]` 속성 선택자만 씁니다(태그 선택자는 `[data-r="marquee"] > div` 하나뿐).
따라서 태그 이름은 의미에 맞게 자유롭게 정할 수 있습니다.

```
data-r="head" → <header>    data-r="page" → <section>    data-r="foot" → <footer>
```

각 면의 첫 `<section>`에 `id="main"`, `<body>` 첫 요소로 `.skip-main` 건너뛰기 링크.
건너뛰기 링크는 `display: none`으로 숨기면 포커스를 못 받습니다. 화면 밖 위치로
빼두고 `:focus`에서 내려오게 해야 합니다.

## 이미지 정책 (2026-08-13 추가)

- **모든 `<img>`에 `width`/`height` 속성**을 답니다. 실제 픽셀값이어야 하며,
  표시 크기는 CSS가 계속 제어합니다. 종횡비를 미리 알려 레이아웃 시프트를 막는 용도입니다.
- **첫 화면 밖 이미지에는 `loading="lazy"`**, 첫 화면 이미지에는 붙이지 마십시오.
  히어로를 지연시키면 LCP가 나빠집니다. 기준선은 두 번째 `<section data-r="page">`입니다.
- **300KB를 넘는 사진은 재인코딩**합니다(JPEG q82 프로그레시브, 투명 PNG는 감색).
  원본은 `_archive/handoff-original/assets/img/`에 있습니다.
- 새 이미지를 넣을 때 이 세 가지를 함께 하십시오. 하나만 빠져도 첫 로드가 무거워집니다.

## 메타데이터 (2026-08-13 추가)

- **`og:image`는 반드시 절대 URL**입니다. 카카오톡·페이스북·슬랙은 상대 경로를
  받지 않아 미리보기가 아예 뜨지 않습니다. 이 사이트의 주된 도달 경로가 링크 공유입니다.
- 공유 카드용 이미지는 `assets/og/<면>.jpg`, 1200×630 고정.
- 면마다 `canonical`, `og:url`, `twitter:card=summary_large_image`를 갖습니다.
  `sitemap.xml`의 URL과 canonical이 일치해야 합니다.
- 홈에는 JSON-LD Organization이 있습니다. 지우지 마십시오(개편 중 한 번 유실된 적 있음).

## 좌우 여백

폭은 풀블리드입니다(중앙 정렬 1440px 컨테이너는 제거됨).
가장자리 여백만 `--gutter` 하나로 관리합니다: 데스크톱 40px / ≤900px 24px / ≤560px 18px.
이 값 하나만 바꾸면 5면 전체에 반영됩니다.

## 앱 화면 교체 컴포넌트 (2026-08-13 추가)

`/product/` Solution 면의 `[data-r="appswap"]`. 앱 화면을 여러 장 늘어놓지 않고 한
자리에서 안쪽만 바꾼다.

- **JS 없음.** 숨긴 `input[type=radio]` 6개 + `:checked ~` 형제 선택자로 동작한다.
  라디오 그룹이라 화살표 키 이동이 공짜로 따라온다. `count.js` 에 아무것도 더하지 마십시오.
- **폰 베젤은 PNG 알파에 이미 들어 있다.** CSS 로 테두리를 다시 그리지 마십시오.
  모서리가 투명하므로 그림자는 사각형이 되는 `box-shadow` 가 아니라 알파를 따라가는
  `filter: drop-shadow()` 를 씁니다.
- **원본이 221×453 뿐이다.** 확대하면 뭉개지므로 `width: 221px` 고정이며 늘리지 않습니다.
  더 큰 원본이 생기기 전에는 크기를 키우지 마십시오.
- **첫 장만 흐름에 두고** 나머지는 `position: absolute` 로 겹칩니다. 그래서 화면을
  바꿔도 높이가 흔들리지 않습니다. 이 구조를 바꾸면 교체할 때마다 지면이 밀립니다.
- **좌우 분할 배치.** 폰을 가운데(2열), 목록을 좌(`apptabs`, 1~3)·우(`apptabs2`, 4~6)에
  둡니다. 목록을 한쪽에만 붙이면 세로로 긴 폰 옆이 비고, 한 줄로 늘이면 지면 오른쪽이
  통째로 빕니다. 항목을 늘리면 두 묶음의 `nth-of-type` 대응을 함께 고쳐야 합니다.
- 860px 이하에서는 좌우 묶음이 가로 칩 한 벌로 접히고 폰이 아래로 갑니다. 칩 높이는
  터치 기준 40px 이상을 유지합니다.

## 디자인 방향 (2026-08-14 명명 — 새 유행을 얹기 전에 여기부터 읽으십시오)

이 사이트의 방향에는 이름이 있습니다. **따뜻한 종이 위의 절제된 스위스 모더니즘**입니다.
장식이 아니라 활자 위계와 여백으로 층위를 만들고, 강조는 오렌지 하나로만 냅니다.

방문자는 제품 사용자가 아니라 **회사를 심사하는 사람**(대회·리그 운영 주체, 기관·지자체,
심사역)입니다. 그래서 화려함보다 **읽기 쉬운 구조**가 먼저입니다. 이 판단이 아래 모든
결정의 근거이며, 유행하는 기법을 얹고 싶을 때 기준이 되는 문장입니다.

고정된 것 — 바꾸려면 근거를 대십시오:

- **모서리 반경 0.** 원형 아이콘의 `50%` 말고는 사이트 전체에 둥근 모서리가 없습니다.
  카드 하나에 `border-radius: 12px` 를 넣는 순간 체계가 깨집니다.
- **강조색 하나.** 오렌지 `#f26a00`, 작은 본문에는 대비를 통과하는 `#b84a00`.
  잉크 `#151812`, 종이 `#f5f0e8`, 실선 `#ded7cc`. 두 번째 강조색을 만들지 마십시오.
- **카드는 뼈대가 아닙니다.** 묶음은 여백과 1px 실선으로 만듭니다. 같은 크기 카드 3개를
  나란히 놓는 구성은 이 사이트의 언어가 아닙니다.
- **활자.** Archivo(라틴 디스플레이) / Noto Sans KR(본문) / IBM Plex Mono(수치) /
  Giants-Inline(브랜드 표기) / **Noto Serif KR** — 세리프는 COMPANY·PROFILE 면에만 씁니다.
  Nanum Myeongjo 를 쓰다가 교체했습니다. 인쇄용 서체라 획이 가늘어 16~19px 본문에서
  무너져 보였습니다. Noto Serif KR 은 화면 렌더링을 전제로 만들어져 같은 크기에서
  획이 고릅니다. **두께는 본문·인용 600, 디스플레이 700** 입니다. 400 을 쓰지 마십시오 —
  그게 원래 깨져 보이던 원인입니다. 세리프를 쓰지 않는 면에서는 폰트를 불러오지도
  마십시오(홈·제품·문의 면이 쓰지도 않으면서 불러오고 있었습니다).
- **아이브로 상한.** 제목 위 대문자 라벨은 페이지 섹션 수의 1/3 이하. 제목이 스스로
  말하면 라벨은 지웁니다. 뉴스 카드의 `선정`·`주관` 같은 **정보성 태그는 아이브로가 아닙니다.**

하지 않기로 한 것 — 반복해서 제안되지만 이 사이트에는 맞지 않습니다:

- **React 계열 모션 라이브러리(Framer Motion 등)와 React 컴포넌트 소스(21st.dev 등).**
  빌드 도구가 없다는 절대 규칙과 충돌합니다. 모션은 CSS 스크롤 구동 애니메이션과
  IntersectionObserver 로 이미 구현되어 있습니다.
- **3D·WebGL 히어로.** B2B 심사자에게 주는 값이 없고 LCP만 나빠집니다.
- **유행하는 ~리즘의 이식.** 글래스모피즘·브루탈리즘·베이지+브라스 프리미엄 팔레트 등을
  덧입히지 마십시오. 방향이 이미 정해져 있습니다.

## 브랜드 마크와 워드마크 (2026-08-14 확정)

- **브랜드 오렌지는 `#ff7400` 입니다.** 정본은 `assets/brand/lockup-orange.png` 입니다.
  `mark-orange.png` 와 `apple-touch-icon.png` 이 `#f26a00` 으로 어긋나 있던 것을
  정본에 맞췄습니다. 브랜드 자산의 오렌지를 다시 바꾸지 마십시오.
- **화면 UI 의 강조색 `#f26a00` 은 별개 체계입니다.** 브랜드 마크 색과 같지
  않아도 됩니다. 작은 본문에는 대비를 통과하는 `#b84a00` 을 씁니다
  (`#ff7400` 은 종이 위 2.39:1, `#f26a00` 은 2.71:1 로 둘 다 소형 텍스트 기준 미달).
- **워드마크는 Ethnocentric 입니다.** 헤더가 Archivo 로 조판되어 있던 것을
  `assets/brand/wordmark-orange.svg` 로 교체했습니다. 정본 lockup 의 자간에
  맞추려고 -0.0194em 을 적용했습니다(폭/캡높이 12.375).

**폰트 라이선스 — 반드시 지키십시오.**
Ethnocentric 은 Typodermic Desktop 라이선스입니다. 동봉된 EULA 가 명시적으로
구분합니다.

- 허용: `logo`, `trademarked logo`, `images (not embedded)`, `web page (not embedded)`
- 금지: **`web page (embedded)`**, `server`, `web template`

즉 **`@font-face` 로 웹폰트 임베딩하는 것은 금지**입니다. 그래서 글자를
윤곽선 경로로 변환한 SVG 자산으로 넣었습니다. 편하다는 이유로 폰트 파일을
`assets/` 에 넣고 `@font-face` 를 선언하지 마십시오. 라이선스 위반입니다.
워드마크 문구를 바꿔야 하면 SVG 를 다시 생성하십시오(fontTools + SVGPathPen).

## 접근성 기준 (2026-08-12 점검 결과 반영 — 되돌리지 마십시오)

- 소형 텍스트의 오렌지는 `#b84a00`(종이 배경 대비 4.61:1). 브랜드 오렌지 `#f26a00`은 **배경·선·점·대형 활자에만** 씁니다. 13px 라벨에 `#f26a00`을 쓰면 2.71:1로 기준 미달입니다.
- 본문 보조 회색은 `#65685f`(5.0:1). `#8b877d`·`#7a776f`·`#6f716b`·`#6b6e64`는 전부 미달이므로 쓰지 마십시오.
- 모바일(≤900px)에서 헤더 내비는 줄바꿈하며 링크 최소 높이 40px입니다.
- 모든 면이 390px 폭에서 가로 스크롤 없이 들어갑니다. 새 요소를 넣은 뒤 반드시 이 폭에서 확인하십시오.

전체 점검 결과는 `AUDIT-2026-08-12.md`에 있습니다.

## 남은 작업

`DESIGN-NOTES.md`의 "남은 작업" 절을 보십시오.
