# 사이트 점검 결과 — 2026-08-12

5면 전체를 데스크톱(1440) / 모바일(390) 두 폭에서 실측 점검했습니다. 항목별 판정과 조치입니다.

## 고친 것 (High)

| 문제 | 실측 | 조치 |
|---|---|---|
| **모바일 가로 스크롤** | 390px 화면에서 문서 폭 598px. 헤더 내비가 455px로 한 줄에 강제 배치되어 페이지 전체를 밀어냄 | 헤더·내비 줄바꿈 허용, 모바일 자간·간격 축소. 5면 모두 373px로 수렴 |
| **기업소개서 모바일 붕괴** | 챕터 인덱스 레일(168px 고정)이 인라인 `display: flex` 때문에 모바일에서도 노출 → 본문 위를 덮음. 장식 워터마크 420px가 화면 밖 63px 침범 | 모바일에서 인덱스·레일 강제 숨김, 워터마크 폭 56vw로 제한 |
| **오렌지 소형 텍스트 대비 미달** | `#f26a00` on `#f5f0e8` = **2.71:1** (13px 라벨 기준 4.5 필요). 아이브로·번호·강조 라벨 62곳 | 소형 텍스트 전용 `#b84a00`(4.61:1), 대형 마침표는 `#cf5600`. 배경·선·점의 브랜드 오렌지는 그대로 |
| **보조 텍스트 대비 미달** | `#8b877d` 3.16 · `#7a776f` 3.94 · `#6f716b` 4.36 · `#6b6e64` 4.58 — 캡션·라벨 53곳 | 전부 `#65685f`(5.0:1)로 통일 |
| **모바일 탭 타깃** | 내비 링크 높이 11~16px | 모바일에서 40px로 확대 |

## 판정: 문제 아님

- **커버의 크림색 텍스트 대비 1.06** — 자동 측정이 사진·그라디언트 배경을 읽지 못해 종이색으로 오판한 값입니다. 실제로는 잉크 베일 위 크림색으로 육안 대비 충분.
- **파트너 로고 마퀴 4350px** — 컨테이너가 클립하는 의도된 좌우 스크롤 스트립.
- **연혁 타임라인 2322px** — 같음(가로 스크롤 UI).

## 남은 권고 (직접 조치 안 함)

1. **모바일 내비가 5개 링크 + Contact 버튼을 두 줄로 감쌉니다.** 지금은 동작하지만, 항목이 더 늘면 햄버거 메뉴가 필요합니다. 지금 넣을지는 판단 필요.
2. **11px 모노 라벨(법인명·대표이사 등)** — 대비는 해결됐지만 크기 자체가 작습니다. 12px로 올리면 밀도가 살짝 느슨해집니다. 취향 문제로 남겨뒀습니다.
3. **기업소개서 모바일에서 챕터 인덱스가 사라집니다.** 8장 카탈로그의 위치 감각이 없어지므로, 모바일 전용 상단 진행 표시(0N / 08)를 넣는 편이 좋습니다.
4. **`h3` 없이 `h2 → h4`로 건너뜁니다** (5면 공통). 스크린리더 목차 구조상 `h3`으로 바꾸는 게 맞습니다. 시각적 변화는 없습니다.
5. 지표 각주·파트너 관계 표기는 여전히 미확보 — `DESIGN-NOTES.md` 우선순위 A 그대로입니다.
